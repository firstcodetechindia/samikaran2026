Further optimize ONLY this current Samikaran Olympiad Replit project for faster real-world loading, especially mobile, without changing any working functionality, routes, business logic, auth flow, registration flow, exam flow, dashboard flow, branding, or existing UI structure.

Current Lighthouse issues to solve next:
- Mobile performance still low
- Render-blocking CSS/fonts
- Efficient cache lifetimes still weak
- Unused JavaScript still high
- Unused CSS still present
- Forced reflow / main-thread work
- Google Tag Manager adding overhead
- Need better LCP on homepage

Work only on performance engineering. Do not redesign the website and do not disturb any current working functionality.

Do these optimizations carefully:

1) Fix render-blocking resources
- Inline only truly critical above-the-fold CSS for homepage hero, top bar, navbar, and first CTA area.
- Load remaining CSS non-blocking.
- Remove any CSS that is not used on homepage initial render.
- Ensure Google Fonts CSS does not block rendering.
- Preconnect only necessary origins.
- Use font-display: swap everywhere.

2) Improve cache policy
- Increase cache lifetime for hashed static assets (JS/CSS/images/fonts) from short TTL to long immutable caching.
- Use:
  - hashed assets: public, max-age=31536000, immutable
  - HTML/documents: no-cache
  - service worker: no-cache
- Keep API caching safe and route-specific only for public endpoints.
- Do not cache auth/private/student/exam-sensitive responses incorrectly.

3) Reduce unused JavaScript
- Audit main homepage bundle and remove everything not required for first paint.
- Lazy load all below-the-fold homepage sections where possible:
  - impact/statistics
  - why choose us
  - olympiad categories
  - about section
  - subjects grid
  - benefits
  - how it works
  - faq
  - news/updates
  - school registration block
  - footer extras
- Defer chat widget / support widget until user interaction or idle time.
- Defer analytics scripts until after first meaningful paint or user interaction where safe.
- If Google Tag Manager is not business-critical for first load, delay it.
- Remove unused icon imports, animation libraries, and component code from homepage bundle.

4) Reduce unused CSS
- Split homepage-critical CSS from global CSS.
- Remove dead utility classes, unused component styles, old route styles, and duplicated styles.
- Avoid loading styles for pages/components not needed on homepage initial visit.

5) Improve mobile LCP
- Identify exact LCP element on homepage and optimize specifically for that element.
- If hero text block is LCP, make sure its fonts and styles load immediately.
- If hero image/logo/illustration is LCP, preload it properly and compress it.
- Prioritize only one hero visual.
- Avoid heavy animation or delayed rendering on hero section.

6) Reduce main-thread and reflow cost
- Minimize layout thrashing and forced reflow in hero/navbar/animated sections.
- Remove expensive repeated measurements, resize handlers, scroll handlers, and layout reads/writes.
- Throttle/debounce any scroll/resize work.
- Replace JS-driven visual effects with pure CSS where possible.
- Reduce framer-motion usage on first viewport; keep only essential animation.
- Disable or simplify non-essential infinite animations on mobile.
- Use transform and opacity only for animations.

7) Optimize DOM size
- Review homepage DOM and reduce unnecessary wrapper divs and duplicated repeated decorative nodes.
- Keep visual result same, but simplify markup where possible.
- Reduce decorative math/science symbol nodes if they increase DOM cost.
- Avoid rendering hidden desktop/mobile duplicates at the same time if possible.

8) Third-party optimization
- Audit Google Tag Manager and any other third-party scripts.
- Delay, lazy load, or gate third-party scripts until consent, interaction, or idle time if possible.
- Do not load third-party code before homepage becomes usable unless absolutely necessary.

9) Smart loading strategy
- Load homepage in this order:
  1. top announcement bar
  2. navbar
  3. hero text + primary CTA
  4. hero essential visual
  5. rest of above-the-fold
  6. all below-the-fold sections lazily
- Use intersection observer for below-the-fold sections.
- Use requestIdleCallback where safe for non-critical enhancements.

10) Keep app-like feel
- Keep fast transitions and responsiveness.
- Keep service worker only for safe static asset caching.
- Do not cache sensitive exam/auth/private data.
- Keep navigation smooth and quick.

11) Verification after changes
After optimization:
- run build
- run app
- verify homepage
- verify mobile nav
- verify login/register still work
- verify public pages still work
- verify no functionality breaks
- compare Lighthouse before/after
- report exactly what further improvements were made

Important safety rules:
- Do NOT redesign
- Do NOT remove working business features
- Do NOT disturb forms, login, register, exam, payment, dashboard, admin, school or student flows
- Do NOT touch backend logic unless needed for safe performance optimization
- Do NOT break SEO/public pages
- Optimize only this Samikaran project and only for faster load/performance

Target:
Push desktop toward 90+ and improve mobile significantly by reducing render blocking, lowering JS/CSS cost, improving cache policy, and reducing main-thread work while keeping the exact same functionality.
