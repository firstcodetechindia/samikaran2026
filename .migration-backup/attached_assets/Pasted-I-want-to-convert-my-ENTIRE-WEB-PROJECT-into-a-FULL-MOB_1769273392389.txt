I want to convert my ENTIRE WEB PROJECT into a FULL MOBILE-APP-LIKE EXPERIENCE using React, Tailwind CSS, animations, gestures, and PWA features.

GOAL:
From Home → Login → Dashboard → All User Panels, the app should feel like a REAL MOBILE APPLICATION, not a website.
Users should forget they are using a browser.

================================================
CORE DESIGN PRINCIPLES
================================================
- Mobile-first (desktop adapts later)
- PWA-style app shell
- Touch-first interactions
- Bottom navigation (no top-heavy menus)
- Smooth transitions, no page reloads
- Robust, premium, production-grade UI

================================================
APP SHELL LAYOUT (GLOBAL)
================================================
Implement a persistent mobile app shell:

- Top App Bar (minimal, optional)
- Main Content Area (scrollable)
- Fixed Bottom Navigation Bar (mobile only)

Rules:
- Bottom navigation always visible on mobile
- Content scrolls above bottom nav
- Respect mobile safe areas (notches, home bar)
- Lock horizontal scrolling
- Use full viewport height (100vh)

================================================
📱 EXACT BOTTOM NAVIGATION (REACT)
================================================
Implement a reusable React Bottom Navigation component:

Tabs (example – configurable by role):
- Home
- Exams
- Performance
- Notifications
- Profile / Menu

Requirements:
- Fixed at bottom on mobile
- Icon + label
- Active tab highlighted
- Smooth tap ripple / feedback
- No page reloads (client-side routing)
- Tab state preserved on refresh
- Different roles can have different tab sets

Behavior:
- Tab click switches main content instantly
- Sub-screens open ABOVE the tab bar
- Bottom nav hides automatically on full-screen modals/exams

================================================
🎞 PAGE TRANSITION ANIMATIONS
================================================
Implement app-like screen transitions:

- Forward navigation:
  → Slide from right to left
- Back navigation:
  → Slide from left to right
- Modal / popup:
  → Fade + scale
- Bottom sheet actions:
  → Slide from bottom

Rules:
- Fast animations (150–250ms)
- Ease-in-out timing
- No heavy or distracting motion
- Animations must feel native, not flashy

================================================
🧭 GESTURE-BASED NAVIGATION
================================================
Enable mobile gestures:

- Swipe right → Go back (where applicable)
- Swipe left → Forward navigation (optional)
- Pull-down → Refresh section (if enabled)
- Tap outside → Close modal/bottom sheet

Rules:
- Gestures must not interfere with exam screens
- Disable gestures during active exams
- Gesture detection should be smooth and lightweight

================================================
HOME → LOGIN → DASHBOARD FLOW
================================================
- Full-screen Home screen
- App-style onboarding (optional)
- Smooth transition to Login screen
- Login screen:
  - Large touch-friendly inputs
  - Minimal UI
  - No clutter
- After login:
  - Slide transition into main app shell
  - Bottom navigation becomes active
- No full page reload anywhere

================================================
USER PANELS (MOBILE VIEW)
================================================
All user panels (Student, Parent, School, Teacher, Partner, future roles except Super Admin):

- Use SAME app shell
- Use SAME bottom navigation pattern
- Replace tables with cards
- Expandable sections instead of long pages
- Smooth animations on data load
- Skeleton loaders instead of spinners

================================================
🚀 PWA MANIFEST & SPLASH CONFIG
================================================
Configure full PWA support:

- App name & short name
- App icons (multiple sizes)
- Theme color & background color
- Display mode: standalone
- Orientation: portrait
- Custom splash screen
- Status bar color matching UI
- Add to Home Screen support
- Offline fallback screens

================================================
PERFORMANCE & UX OPTIMIZATION
================================================
- Tailwind optimized classes
- No unnecessary re-renders
- Lazy load heavy screens
- Cache static assets
- Fast initial load
- Smooth scrolling

================================================
ACCESSIBILITY & SAFETY
================================================
- Large touch targets
- High contrast text
- Reduced motion support
- Keyboard navigation where needed
- Disable gestures during exams
- No accidental navigation during tests

================================================
FINAL EXPECTATION
================================================
This project should feel like:
- A real MOBILE APPLICATION
- Installed from Play Store / App Store
- Smooth, robust, premium
- Highly intuitive navigation
- World-class education SaaS UX

No website-like headers.
No messy menus.
No reloads.

Deliver a PRODUCTION-READY, MOBILE-APP-LIKE UI with React, Tailwind, animations, gestures, and full PWA support.
