COMPREHENSIVE SEO & PUBLIC WEBSITE OPTIMIZATION:

I audited the public website and found CRITICAL SEO issues that need immediate fixing for Google indexing and search visibility:

## CRITICAL SEO ISSUES TO FIX:

### 1. META TAGS MISSING/INCOMPLETE:
- No Open Graph (OG) meta tags for social media sharing (og:title, og:description, og:image, og:url, og:type)
- No Twitter Card tags (twitter:card, twitter:title, twitter:description, twitter:image)
- Missing canonical URL tags
- Missing schema.org structured data (Organization, WebSite, BreadcrumbList)
- Page title exists but needs optimization per page

### 2. SITEMAP.XML NOT WORKING:
- /sitemap.xml fails to load (timeout error)
- Robots.txt points to non-existent sitemap URL
- Need dynamic sitemap generation for all pages

### 3. TECHNICAL SEO GAPS:
- No JSON-LD structured data for rich snippets
- Missing alt text on images
- No hreflang tags (for future multi-language support)
- Missing meta robots tags on dynamic pages

### 4. PERFORMANCE & CRAWLABILITY:
- Loading states might block crawlers
- No prerendering for search engines
- Missing RSS feed for blog

## IMPLEMENTATION TASKS:

**TASK 1: Add Complete Meta Tags System**
- Create `client/src/utils/seo.ts` with meta tag helper functions
- Add per-page meta configuration:
  - Home: "India's #1 AI-Powered Olympiad Platform | Samikaran"
  - About: "About Samikaran Olympiad - AI-Powered Competitive Exams"
  - Olympiads: "Online Olympiad Exams for Class 1-12 | 20+ Subjects"
- Add og:image (1200x630px) - use hero image or create one
- Add Twitter Card meta tags
- Update `client/src/pages/Home.tsx` and all pages with Helmet or similar for dynamic meta

**TASK 2: Fix & Generate sitemap.xml**
- Create `/server/sitemap-generator.ts`
- Generate XML sitemap with:
  - All static pages (/, /about, /olympiads, /blog, /contact, /faq, /partners, /awards, /brand)
  - All olympiad pages dynamically from DB
  - All blog posts (if any)
  - Update frequencies and priorities
- Add route: GET /sitemap.xml serving generated XML
- Update robots.txt sitemap URL to correct domain

**TASK 3: Add Structured Data (Schema.org JSON-LD)**
- Add Organization schema on homepage
- Add WebSite schema with sitelinks searchbox
- Add BreadcrumbList on inner pages
- Add Event schema for upcoming olympiads
- Add FAQPage schema on FAQ page

**TASK 4: Image Optimization**
- Add descriptive alt text to all images
- Ensure hero image has proper alt: "Students celebrating achievement at Samikaran Olympiad"
- Add lazy loading attributes

**TASK 5: robots.txt Enhancement**
- Fix sitemap URL (currently points to samikaran olympiad.com but should point to actual deployed domain)
- Add Crawl-delay if needed

**TASK 6: Add RSS Feed for Blog**
- Create /blog/rss.xml endpoint
- Generate RSS 2.0 feed with blog posts

**TASK 7: Performance Optimizations**
- Add preconnect to external domains
- Ensure all pages have proper <title> and <meta name="description">
- Add viewport meta tag (if missing)

Implement all 7 tasks now. Start with creating the SEO utility file, then fix sitemap, then add meta tags to all pages, then structured data.